/* @flow */

// State
const initialState = {
};

// Actions

// Action Creators

// Reducer
export default function reducer(state: Object = initialState, action: Object = {}) {
  switch (action.type) {
    default: return state;
  }
}
